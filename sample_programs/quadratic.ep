def quad(a, b, c) := let d := sqrt(b^(2.0) - 4.0 * a * c) in
                     let x1 := (-b + d) / (2.0 * a) in
                     let x2 := (-b - d) / (2.0 * a) in
                     return {x1, x2}
const {x1, x2} := quad(3, 56, 32)
x1
x2
{x1, x2}